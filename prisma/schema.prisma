generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id        String   @id @default(uuid())
  nome      String
  email     String   @unique
  senha     String
  perfil    String
  ra        String?  @unique
  
  logs      AttendanceLog[]
  turmas    Turma[]  @relation("Matriculas") // Turmas que ele estuda
  turmasLecionadas Turma[] @relation("Professor") // NOVO: Turmas que ele ensina
}

model Turma {
  id            String     @id @default(uuid())
  nome          String
  latitude      Float
  longitude     Float
  raioMetros    Int        @default(20)
  totalAulas    Int        @default(20)
  
  logs          AttendanceLog[]
  alunos        User[]     @relation("Matriculas")
  
  professorId   String?    // NOVO: ID do Professor
  professor     User?      @relation("Professor", fields: [professorId], references: [id]) // NOVO
}

// O model AttendanceLog continua igual, não precisa mexer.
model AttendanceLog {
  id          String   @id @default(uuid())
  dataHora    DateTime @default(now())
  latitude    Float
  longitude   Float
  statusSiga  String   @default("PENDENTE")
  dataExclusao DateTime
  
  userId      String
  user        User     @relation(fields: [userId], references: [id])
  turmaId     String
  turma       Turma    @relation(fields: [turmaId], references: [id])
}